// Component Scaffold

import ComponentAccordion from './component-accordion';

if ( window.TenUp )
	window.TenUp = [...window.TenUp];
else
	window.TenUp = [];

window.TenUp.ComponentAccordion = ComponentAccordion;